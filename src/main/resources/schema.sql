CREATE TABLE IF NOT EXISTS ACCOUNT (
    EMPLOYEE_ID VARCHAR(10) PRIMARY KEY
    , PASSWORD TEXT NOT NULL
    , IS_ADMIN CHAR(1) NOT NULL
);

CREATE TABLE IF NOT EXISTS BIRTHPLACE (
    CITY_ID VARCHAR(3) PRIMARY KEY
    , CITY VARCHAR(3)
);

CREATE TABLE IF NOT EXISTS GENDER (
    GENDER_ID VARCHAR(3) PRIMARY KEY
    , GENDER VARCHAR(3)
);

CREATE TABLE IF NOT EXISTS BLOOD_TYPE (
    BLOOD_TYPE_ID VARCHAR(3) PRIMARY KEY
    , BLOOD_TYPE VARCHAR(3)
);

CREATE TABLE IF NOT EXISTS PROFILE (
    EMPLOYEE_ID VARCHAR(10) REFERENCES ACCOUNT
    , PICTURE BYTEA DEFAULT NULL
    , NAME VARCHAR(256) NOT NULL
    , HIRE_DATE CHAR(6) DEFAULT NULL
    , IS_NEWCOMER CHAR(1) NOT NULL
    , GENDER_ID VARCHAR(3) DEFAULT NULL REFERENCES GENDER(GENDER_ID)
    , BLOOD_TYPE_ID VARCHAR(3) DEFAULT NULL REFERENCES BLOOD_TYPE(BLOOD_TYPE_ID)
    , SALES_POINT VARCHAR(256) DEFAULT NULL
    , HOBBY VARCHAR(256) DEFAULT NULL
    , BIRTHPLACE CHAR(2) DEFAULT NULL --データモデル上出身地だが、出身テーブルの都道県IDと結びつかないといけないのでは
    , PRESENTATION BYTEA DEFAULT NULL
);

CREATE TABLE IF NOT EXISTS CAREER (
    EMPLOYEE_ID VARCHAR(10) REFERENCES ACCOUNT
    , START_DATE CHAR(6) NOT NULL
    , END_DATE CHAR(6) DEFAULT NULL
    , PROJECT VARCHAR(256) NOT NULL
    , PROJECT_CONTENT VARCHAR(3000) DEFAULT NULL
);

CREATE TABLE IF NOT EXISTS REGISTRATION_UPDATE (
    EMPLOYEE_ID VARCHAR(10) REFERENCES PROFILE(EMPLOYEE_ID)
    , UPDATE_DATE TIMESTAMP NOT NULL
);

CREATE TABLE IF NOT EXISTS TEAM (
    TEAM_ID VARCHAR(3) PRIMARY KEY
    , TEAM_NAME VARCHAR(256) NOT NULL
);

CREATE TABLE IF NOT EXISTS BELONG (
    EMPLOYEE_ID VARCHAR(10) PRIMARY KEY
    , TEAM_ID VARCHAR(3) NOT NULL REFERENCES TEAM(TEAM_ID)
);
